{
  "name": "cloudns-cloudformation-sync",
  "version": "2.0.0",
  "description": "Generate ClouDNS DNS records from AWS CloudFormation exports",
  "main": "lib/cloudns-cloudformation-sync.js",
  "bin": {
    "cloudns-cloudformation-sync": "bin/cloudns-cloudformation-sync"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "directories": {
    "lib": "lib"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc",
    "prepare": "tsc",
    "ncu": "ncu -u && npm install",
    "ncucheck": "ncu"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cloudeninc/cloudns-cloudformation-sync.git"
  },
  "keywords": [
    "cloudns",
    "dns",
    "aws",
    "cloudformation",
    "cloudfront",
    "api-gateway",
    "sync"
  ],
  "author": "Kenneth Falck <kennu@clouden.net>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/cloudeninc/cloudns-cloudformation-sync/issues"
  },
  "homepage": "https://github.com/cloudeninc/cloudns-cloudformation-sync#readme",
  "dependencies": {
    "@aws-sdk/client-cloudformation": "^3.1127.0",
    "@aws-sdk/client-ssm": "^3.1127.0"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "typescript": "^7.0.2"
  }
}
